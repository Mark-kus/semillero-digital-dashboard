"use client";

// Placeholder component - will be moved from existing components
interface StudentProgressProps {
  userRole: string;
}

export function StudentProgress({userRole}: StudentProgressProps) {
  return (
    <div className="p-6">
      <h2 className="mb-4 text-2xl font-bold">Progreso de Estudiantes</h2>
      <p>Componente en proceso de migración...</p>
    </div>
  );
}
